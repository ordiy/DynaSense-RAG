--- run_benchmark.py
+++ run_benchmark.py
@@ -124,7 +124,10 @@
 print(f"\nRunning benchmark on {len(test_queries)} queries...")
 
 def calc_recall(retrieved_docs, expected_id, k):
-    retrieved_ids = [d.metadata.get("doc_id") for d in retrieved_docs[:k]]
+    # Ensure types match (LanceDB sometimes returns int as float or string in metadata)
+    retrieved_ids = []
+    for d in retrieved_docs[:k]:
+        retrieved_ids.append(int(d.metadata.get("doc_id", -1)))
     return 1 if expected_id in retrieved_ids else 0
 
 results = {
